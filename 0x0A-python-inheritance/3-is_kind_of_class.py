#!/usr/bin/python3
"""
    A module that checks if object is
    an instance of a class defined or inherited
"""


def is_kind_of_class(obj, a_class):
    """
        function or method that checks if object is an
        instance of a class that inherited from
    """
    return (isinstance(obj, a_class))
